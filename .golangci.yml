run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - gosec
    - gosimple
    - errcheck
issues:
  exclude-rules:
    - path: _test.go
      linters:
        - errcheck
